
{
  "entities": {
    "Webhook": {
      "title": "Webhook",
      "description": "Entrada de sinal recebida pelo sistema.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "method": { "type": "string" },
        "headers": { "type": "object" },
        "payload": { "type": "object" },
        "interpretation": {
          "type": "object",
          "properties": {
            "summary": { "type": "string" },
            "codes": { "type": "array", "items": { "type": "string" } }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "timestamp", "payload", "createdAt"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/webhooks/{webhookId}": {
      "schema": "Webhook",
      "description": "Coleção de sinais recebidos via API."
    }
  }
}
